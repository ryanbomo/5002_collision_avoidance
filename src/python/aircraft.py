import math, re

class Aircraft:
  neighbors = {}
  
  def __init__(self, myInfo):
    myInfo[0] = self.id
    myInfo[1] = self.x
    myInfo[2] = self.y
    myInfo[3] = self.z
    myInfo[4] = self.speed
    myInfo[5] = self.cardinality
    
  def listen(self):
    return thing
  
  def broadcast(self):
    return thing
  
  def check_neighbors(self):
    return thing